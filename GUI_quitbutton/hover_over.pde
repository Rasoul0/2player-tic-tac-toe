/*
  void hover() {
  //
  //Reset, rect(resetX, scoreYnameX, resetWidth, scoreHeightXO*0.5);
  if (mouseX >= resetX  && mouseX <= resetX+resetWidth && mouseY >= scoreYnameX && mouseY <= (scoreYnameX+(scoreHeightXO*0.5)) ) {
    noStroke();
    rect(resetX, scoreYnameX, resetWidth, scoreHeightXO*0.5);
    stroke(1);
    textDraw(reset, titleFont, height, black, CENTER, CENTER, resetX, scoreYnameX, resetWidth, scoreHeightXO*0.5);
  } else {
    fill(red);
    noStroke();
    rect(resetX, scoreYnameX, resetWidth, scoreHeightXO*0.5);
    stroke(gold);
    textDraw(reset, titleFont, height, black, CENTER, CENTER, resetX, scoreYnameX, resetWidth, scoreHeightXO*0.5);
  }
  //
   rect(scoreA, InsaneY, scoreWidth, InsaneHeight);
  if (twoPlayer2 == false && twoPlayerAI == false && mouseX >= scoreA  && mouseX <= InsaneY+scoreWidth && mouseY >= InsaneY && mouseY <= InsaneY+InsaneHeight) {
    fill(red);
    noStroke();
    rect(scoreA, InsaneY, scoreWidth, InsaneHeight);
    stroke(1);
    textDraw(playerMode, titleFont, height, black, CENTER, TOP, scoreA, InsaneY, scoreWidth, InsaneHeight);
  } else {
    fill(black);
    noStroke();
    rect(scoreA, InsaneY, scoreWidth, InsaneHeight);
    stroke(1);
    textDraw(playerMode, titleFont, height, black, CENTER, TOP, scoreA, InsaneY, scoreWidth, InsaneHeight);
  }
  
  //
  //Easy, rect(easyX, easyY, easyWidth, easyHeight);
  if (mouseX >= NoobX  && mouseX <= NoobX+NoobWidth && mouseY >= NoobY && mouseY <= NoobY+NoobHeight) {
    fill(red);
    noStroke();
    rect(NoobX, NoobY, NoobWidth, NoobHeight);
    stroke(1);
    textDraw(easy, titleFont, height, black, CENTER, CENTER, NoobX, NoobY, NoobWidth, NoobHeight);
  } else {
    fill(gold);
    noStroke();
    rect(NoobX, NoobY, NoobWidth, NoobHeight);
    stroke(1);
    textDraw(easy, titleFont, height, black, CENTER, CENTER, NoobX, NoobY, NoobWidth, NoobHeight);
  }
  //
  //Medium, rect(mediumX, mediumY, mediumWidth, mediumHeight);
  if (mouseX >= mediumX  && mouseX <= mediumX+mediumWidth && mouseY >= mediumY && mouseY <= mediumY+mediumHeight) {
    fill(red);
    noStroke();
    rect(mediumX, mediumY, mediumWidth, mediumHeight);
    stroke(1);
    textDraw(medium, titleFont, height, black, CENTER, CENTER, mediumX, mediumY, mediumWidth, mediumHeight);
  } else {
    fill(gold);
    noStroke();
    rect(mediumX, mediumY, mediumWidth, mediumHeight);
    stroke(1);
    textDraw(medium, titleFont, height, black, CENTER, CENTER, mediumX, mediumY, mediumWidth, mediumHeight);
  }
  //
  //Impossible, rect(impossibleX, impossibleY, impossibleWidth, impossibleHeight);
  if (mouseX >= InsaneX  && mouseX <= InsaneX+InsaneWidth && mouseY >= InsaneY && mouseY <= InsaneY+InsaneHeight) {
    fill(red);
    noStroke();
    rect(InsaneX, InsaneY, InsaneWidth, InsaneHeight);
    stroke(1);
    textDraw(impossible, titleFont, height, black, CENTER, CENTER,InsaneX, InsaneY, InsaneWidth, InsaneHeight);
  } else {
    fill(gold);
    noStroke();
    rect(InsaneX, InsaneY, InsaneWidth, InsaneHeight);
    stroke(1);
    textDraw(impossible, titleFont, height, black, CENTER, CENTER, InsaneX, InsaneY, InsaneWidth, InsaneHeight);
  }
}
  */
  
  
  
  
  
  
  
